<td colspan="2">
  <%= form_for [user, item], remote: true do |f| %>
    <%= f.text_field :description, class: 'form-control', placeholder: "Enter new task" %>
    </td> <!-- Really weird indenting -->
    <td>
    <%= f.submit "Save", class: 'btn btn-success' %>
  <% end %>
</td>
